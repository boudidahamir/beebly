{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reponse.date ? reponse.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reponse.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_index') }}"><button class="btn btn-primary"> back to list </button></a>

    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}"><button class="btn btn-warning"> Edit </button></a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
