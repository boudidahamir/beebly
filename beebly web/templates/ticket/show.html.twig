{% extends 'base.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ticket.prix }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ ticket.type }}</td>
            </tr>
            <tr>
                <th>libelle evenement</th>
                <td>{{ ticket.idEvenement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ticket_index') }}"><button class="btn btn-primary"> back to list </button></a>

    <a href="{{ path('app_ticket_edit', {'id': ticket.id}) }}"><button class="btn btn-warning"> Edit </button></a>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
