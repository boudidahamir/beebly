{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
    <h1>Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ livre.titre }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ livre.categorie }}</td>
            </tr>
            <tr>
                <th>DatePublication</th>
                <td>{{ livre.DatePublication ? livre.DatePublication|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src= {{ asset('uploads/'~ livre.image )}} width="100%" /> </td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ livre.prix }}</td>
            </tr>
            <tr>
                <th>DescriptionEtatLivre</th>
                <td>{{ livre.DescriptionEtatLivre }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ livre.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livre_index') }}">Retour à la liste</a>

    <br>

    <a href="{{ path('app_livre_edit', {'id': livre.id}) }}">Modifier le livre</a>


    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}
